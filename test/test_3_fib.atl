include "std.atl"

fib (i64 num) -> i64 {
    if num == 0 {
        -> 0
    } else if num == 1 {
        -> 1
    } else {
        -> fib(num - 1) + fib(num - 2)
    }
}

main() -> i64 {
    i64 :: res = fib(10)
    putint(res)
    -> 0
}
