include "std.atl"

fn fib (i64 num) -> i64 {
    if num == 0 {
        -> 0
    } else if num == 1 {
        -> 1
    } else {
        -> fib(num - 1) + fib(num - 2)
    }
}

fn main() -> i64 {
    i64 :: res = fib(10)
    puti(res)
    -> 0
}
